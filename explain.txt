// create a account on razorpay
// under setting > API kye

// integrate razorpay to client side
// create order id from server 
    => create api as get-order-id


// Google Login 
    # login to https://console.cloud.google.com/
    # create a project
    # OAuth Consent Screen (External)
    # Credentials   
        http://localhost
        http://localhost:3000

    # after successfully login
        you will get jwt token
        JWT => JSON Web Token



        echo "# Clone-Zomato" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/2001Manikandan/Clone-Zomato.git
git push -u origin main
         